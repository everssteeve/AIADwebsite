Implemente la fonctionnalite suivante :

**Arguments fournis :** $ARGUMENTS

Analyse les arguments ci-dessus pour identifier le chemin de la SPEC a implementer.
Le format attendu est : `US-XXX T-XXX-nom-tache` (ex: `US-004 T-004-B4-helpers-navigation`)

**Fichiers a lire OBLIGATOIREMENT avant de commencer :**
- SPEC : lis le fichier `docs/specs/<US-identifiee>/<T-identifiee>.md`
- Contexte : lis le fichier `CLAUDE.md`
- Architecture : lis le fichier `docs/ARCHITECTURE.md`

**Instructions :**
1. Lis la SPEC en entier avant de commencer
2. Cree les fichiers dans l'ordre indique
3. Respecte strictement les interfaces TypeScript definies
4. Gere tous les cas d'erreur documentes
5. N'ajoute AUCUNE fonctionnalite non specifiee

Montre-moi ton plan d'implementation avant de coder.
