Genere une SPEC complete pour cette tache.

**Arguments fournis :** $ARGUMENTS

Analyse les arguments ci-dessus pour identifier :
1. Le numero de User Story (ex: US-004)
2. La description de la tache atomique (ex: T-004-B4 Helpers navigation)

**Contexte a lire OBLIGATOIREMENT avant de generer :**
- User Story : lis le fichier `docs/specs/<US-identifiee>/spec-<US-identifiee>.md`
- Architecture : lis le fichier `docs/ARCHITECTURE.md`
- AGENT-GUIDE : lis le fichier `CLAUDE.md`
- Template de spec existant : cherche un exemple de spec deja generee dans `docs/specs/` pour respecter le format etabli

**Exigences :**
1. Utilise le template SPEC du projet (base-toi sur les specs existantes dans docs/specs/)
2. Definis les types TypeScript complets
3. Liste tous les cas limites identifiables
4. Fournis des exemples concrets entree/sortie
5. Specifie les tests attendus avec leurs assertions

**Format :** Markdown compatible avec le template docs/specs/

Think hard.
